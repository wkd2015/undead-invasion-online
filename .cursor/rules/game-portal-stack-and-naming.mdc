---
description: 技术栈、目录命名、单语英文根路径、R2 静态资源
alwaysApply: true
---

# 技术栈与工程约定

## 技术栈（固定）

- **Next.js（App Router）**、**TypeScript**、**Tailwind CSS**、**Shadcn UI**。

## 命名规范

- **目录**：`kebab-case`（例：`game-container`、`related-games`）。
- **组件文件**：`PascalCase`（例：`GameContainer.tsx`）。
- **工具/模块文件**：`camelCase`（例：`formatDate.ts`）。

## 语言与 URL（本项目）

- **纯英文站**：主内容与 SEO 页面使用**根路径** `/`（无 `/en` 等 locale 前缀）。
- 若未来扩展多语言，再采用子路径（如 `/ko/`）并单独规划 sitemap / canonical；**禁止**子域名切语言、禁止仅靠前端切换而无独立 URL。

## 静态资源与 R2

- 游戏 HTML 镜像、大图、视频等大流量静态资源**必须**走单独源站，并通过 Next **`assetPrefix`**（或等价 CDN 前缀）引用，托管在 **Cloudflare R2**，以降低 Vercel 带宽成本。
- 具体前缀由环境变量配置；不在代码中硬编码密钥或桶凭证。
