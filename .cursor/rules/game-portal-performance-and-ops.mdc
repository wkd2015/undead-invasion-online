---
description: SSR/SSG、404、广告 CLS、移动优先、sitemap
alwaysApply: true
---

# 性能、渲染与运维

## 服务端渲染与收录

- **核心文字内容**必须在 **SSR 或 SSG** 生成的 HTML 中直接可见（「查看网页源代码」可见）。
- **禁止**将需要收录的主正文仅放在客户端 hydration 之后（纯 CSR 主内容）。

## Cloudflare Pages 与 404

- 面向 **Cloudflare Pages** 的部署需包含根目录 **`public/404.html`**（或平台文档要求的等价路径），避免无效路径返回 **200** 导致爬虫死循环。
- Next/Vercel 与 Pages 并存时，分别满足各平台的 404 行为要求。

## 广告与 CLS

- 预留广告位时使用**固定尺寸容器**，放在侧栏或内容区块之间；**不得遮挡**游戏主操作区，**不得诱导误触**。
- 避免因广告插入导致 **CLS** 恶化。

## 移动端（Mobile First）

- **移动优先**设计；首屏必须呈现游戏入口。
- 按钮与可点区域符合**触控**尺寸与间距建议。

## Sitemap

- 每新增游戏落地页，必须**自动更新** `sitemap.xml`（构建时生成或 CI 生成均可，需可复现）。
- 发布后通过 **API 或 Google Search Console** 提交/通知（在规则与文档中保留步骤，具体密钥由运维配置）。
