---
description: Technical SEO — TDK、标题层级、canonical、OG、Schema、alt、关键词密度
globs: "**/app/**/*.{tsx,ts}"
alwaysApply: false
---

# Technical SEO 约束

## TDH 与元信息

- **Title**：**50–60** 字符；**核心关键词靠前**。
- **Description**：**150–160** 字符；包含明确 **CTA**（行动号召）。
- **Headings**：
  - **有且仅有 1 个 H1**，且必须包含**核心关键词**。
  - **H2–H6** 层级连贯，**禁止跳级**（例如 H1 后直接 H4）。

## 元标签（必选）

- **canonical**：指向该页的规范 URL（本项目首页为站点根 URL，无 locale 前缀）。
- **Open Graph** 全套合理字段（`og:title`、`og:description`、`og:url`、`og:image` 等）。
- **Twitter Card** 标签（与 OG 信息一致、无矛盾）。

## 关键词密度

- 正文核心关键词密度控制在 **3%–5%**；发布前用工具或脚本复核，避免堆砌与不自然重复。

## 图片

- 所有 **`<img>` / Next `Image`** 必须有**描述性 `alt`**，与画面内容相关，非空泛填充。

## 结构化数据（JSON-LD）

- 每页强制注入 **`SoftwareApplication`** 或 **`VideoGame`** 类型的 JSON-LD（与页面内容一致，字段真实可校验）。
- 使用 Next Metadata API 或独立 `<script type="application/ld+json">` 输出，确保在**服务端 HTML** 中可见。
